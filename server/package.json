{
  "name": "frenchy-bridge",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "build": "tsc",
    "build:client": "cd ../client && rm -rf node_modules package-lock.json && npm install && npx vite build",
    "build:all": "rm -rf node_modules package-lock.json && npm install && npm run build:client && npm run build"
  },
  "dependencies": {
    "@google/genai": "^1.15.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "ws": "^8.19.0",
    "@types/express": "^4.17.21",
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.10",
    "typescript": "~5.8.2"
  },
  "devDependencies": {
    "tsx": "^4.21.0"
  }
}
